{
    "build": {
      "env": {
        "PIP_CACHE_DIR": ".vercel/cache/pip"
      },
      "commands": {
        "installCommand": "pip install --cache-dir=$PIP_CACHE_DIR -r requirements.txt",
        "buildCommand": "mkdocs build"
      }
    }
  }
  